{
  "name": "defi-flashloan-system",
  "version": "1.0.0",
  "description": "DeFi flash loan arbitrage system with Sharia-compliant Mudarabah pool on Arbitrum",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.js --network arbitrumSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network arbitrum",
    "verify": "hardhat run scripts/verify.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@aave/core-v3": "^1.19.3",
    "@openzeppelin/contracts": "^5.0.0",
    "@uniswap/v3-periphery": "^1.4.4",
    "dotenv": "^16.3.1"
  }
}
