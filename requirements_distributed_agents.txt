# Requirements for Distributed Agent Architecture
# Core distributed system dependencies
redis>=4.5.0
python-consul>=1.1.0
etcd3>=0.12.0
PyYAML>=6.0

# System monitoring
psutil>=5.9.0
aiohttp>=3.8.0

# Blockchain dependencies (if not already installed)
web3>=6.0.0
eth-account>=0.8.0

# Additional dependencies for production
asyncio-mqtt>=0.13.0  # For MQTT messaging
prometheus-client>=0.16.0  # For metrics
structlog>=22.3.0  # For structured logging
tenacity>=8.2.0  # For retry logic
cryptography>=39.0.0  # For encryption
