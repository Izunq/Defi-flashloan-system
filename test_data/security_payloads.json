{
  "sql_injection": [
    "'; DROP TABLE users; --",
    "' OR '1'='1",
    "' UNION SELECT * FROM passwords --"
  ],
  "xss": [
    "<script>alert('xss')</script>",
    "<img src=x onerror=alert(1)>",
    "javascript:alert('xss')"
  ],
  "command_injection": [
    "; rm -rf /",
    "| cat /etc/passwd",
    "&& echo vulnerable"
  ]
}